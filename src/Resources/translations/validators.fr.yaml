setono_sylius_analytics:
    container:
        channels:
            min_count: You must select {{ limit }} or more channels.
        measurement_id:
            unique: You already have one container with this container id
    property:
        channels:
            min_count: Vous devez sélectionner au moins {{ limit }} canal(aux) ou plus.
        measurement_id:
            unique: Vous avez déjà une propriété avec ce Tracking id
